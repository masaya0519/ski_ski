<div class="container">
  <div class="row mt-3">

　　<h4 class="col-md-3 bg-light text-center m-4">スキー場一覧</h4>
　　<div class="col-md-3 m-4"></div>
　　<div class="col-md-3 m-4">
      <%= link_to new_admin_ski_path, class:"btn btn-outline-secondary" do %>
        <i class="fas fa-plus fa-1x"></i>
      <% end %>
    </div>
　</div>
  <div class="row">

    <% @skis.each do |ski| %>
    <div class="row"><%= link_to ski.ski_name, admin_ski_path(ski.id) %></div>
    <div class="row">
      <div class="col-7">
        <%= ski.explanation %>
      </div>
      <div class="col-5">
        <div class="row">
          <div class="col-4">標高</div>
          <div class="col-2"><%= ski.elevation %>m</div>
        </div>
        <div class="row"><%= ski.running_distance %>m</div>
        <div class="row"><%= ski.cover %>cm</div>
        <div class="row"><%= ski.quality%></div>
        <div class="row"><%= ski.genre.genre_name %></div>
      </div>
    <% end %>
    </div>
  </div>
</div>
